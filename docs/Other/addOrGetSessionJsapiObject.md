### ``addOrGetSessionJsapiObject(jsapi, sessionId)``
Adds ``jsapi`` to a cache under ``sessionId`` or fetches cache for ``sessionId`` if it exists.
- `jsapi` `<Object>`
- `sessionId` `<String>`

```js
pvh.addOrGetSessionJsapiObject(jsapi, 'qwfd1f13g');
```