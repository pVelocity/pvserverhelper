### ``removeSessionJsapiObject(jsapi, sessionId)``
Fetches cache for ``sessionId`` if it exists and removes it or returns ``jsapi``.
- `jsapi` `<Object>`
- `sessionId` `<String>`

```js
pvh.removeSessionJsapiObject(jsapi, 'qwfd1f13g');
```